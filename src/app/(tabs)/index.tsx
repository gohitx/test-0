import { Text, View } from 'react-native';
import { screenStyles } from '../../styles/theme/screen';

export default function HomeScreen() {
  return (
    <View style={screenStyles.container}>
      <Text style={screenStyles.title}>Home 2</Text>
    </View>
  );
}
